{
  "builds": {
    "production": {
      "buildCommand": "npm install --no-optional && npm run build",
      "distDir": "dist",
      "nodeVersion": "18.x"
    },
    "preview": {
      "buildCommand": "npm install --no-optional && npm run build",
      "distDir": "dist",
      "nodeVersion": "18.x"
    }
  }
} 
