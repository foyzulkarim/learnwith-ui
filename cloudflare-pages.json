{
  "builds": {
    "production": {
      "buildCommand": "npm cache clean --force && npm install --no-optional && chmod +x ci-prebuild.js && ROLLUP_SKIP_NODEJS=true npm run build",
      "distDir": "dist",
      "nodeVersion": "18.x"
    },
    "preview": {
      "buildCommand": "npm cache clean --force && npm install --no-optional && chmod +x ci-prebuild.js && ROLLUP_SKIP_NODEJS=true npm run build",
      "distDir": "dist",
      "nodeVersion": "18.x"
    }
  }
} 
